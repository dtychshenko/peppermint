import { MIME_TYPES } from "@mantine/dropzone";

/**
 * Maximum file size upload limit in MB
 */
export const UPLOAD_SIZE_LIMIT_MB = 5;

/**
 * Accepted file types for upload
 */
export const UPLOAD_ACCEPT = [MIME_TYPES.csv];
